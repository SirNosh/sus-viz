# The Distance Remaining

A 2024 public-data visualization of Johns Hopkins sustainability progress and institutional commitments.

## Development

```bash
npm install
npm run dev
```

## Validation

```bash
npm run lint
npm test
```

The production build uses `/sus-viz/` as its base path and deploys to GitHub Pages through `.github/workflows/deploy-pages.yml`.
